# Blue/Green Deployment Configuration

# Container Images (required - set these to your actual image URLs)
BLUE_IMAGE=your-blue-image:latest
GREEN_IMAGE=your-green-image:latest

# Active pool: 'blue' or 'green'
ACTIVE_POOL=blue

# Release identifiers (shown in X-Release-Id header)
RELEASE_ID_BLUE=blue-v1.0.0
RELEASE_ID_GREEN=green-v1.0.0

# Application port (optional, defaults to 3000)
PORT=3000