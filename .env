# Docker image references for Blue and Green services
BLUE_IMAGE=yimikaade/wonderful:devops-stage-two
GREEN_IMAGE=yimikaade/wonderful:devops-stage-two

# Release identifiers passed to app containers
RELEASE_ID_BLUE=v1.0.0-blue
RELEASE_ID_GREEN=v1.0.0-green

# Default: 3000 (matches Node.js convention)
PORT=3000

# Active pool for future templating (blue or green)
ACTIVE_POOL=blue

# ============================================
# Monitoring and Alerting Configuration
# ============================================

# Slack incoming webhook URL for alerts (required for alerts)
# Get this from: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# Error rate threshold as percentage (default: 2)
# Alert when 5xx error rate exceeds this percentage over the sliding window
ERROR_RATE_THRESHOLD=2

# Sliding window size in number of requests (default: 200)
# Error rate is calculated over the last N requests
WINDOW_SIZE=200

# Alert cooldown in seconds (default: 300)
# Minimum time between repeated alerts of the same type
ALERT_COOLDOWN_SEC=300

# Maintenance mode flag (default: false)
# Set to 'true' to suppress alerts during planned maintenance/toggles
MAINTENANCE_MODE=false